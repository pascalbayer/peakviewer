/** Bundle entry for check_align.mjs: the alignment core, importable from Node. */
export {
  DEFAULT_ALIGN, DEFAULT_PROFILE, DEFAULT_SKYLINE,
  extractSkyline, horizonProfile, matchSkyline, profileAt,
} from '../src/core/align';
export { HeightField } from '../src/core/heightfield';
